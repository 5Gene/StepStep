<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.StepStep" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StepStep" parent="Base.Theme.StepStep" />

    <style name="Theme.StepStep.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.StepStep.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.StepStep.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>